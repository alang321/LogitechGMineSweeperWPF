using LogitechGMineSweeper;

namespace WinInterop
{
    internal class WindowInteropHelper
    {
        private MainWindow mainWindow;

        public WindowInteropHelper(MainWindow mainWindow)
        {
            this.mainWindow = mainWindow;
        }
    }
}